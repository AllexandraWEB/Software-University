function numberN(inputArray) {
    let inputNumber = Number(inputArray[0]);

    for (let currentNumber = inputNumber; currentNumber > 0; currentNumber--) {
        console.log(currentNumber);
    }
}


//numberN(["5"]);